/*
 * 默认日记内容用于“点击创建即落库”的入口。
 * 标题和正文都保持非空，满足 main process 的创建校验。
 */
export const DEFAULT_DIARY_TITLE_PREFIX = "未命名日记";

export const DEFAULT_DIARY_MARKDOWN = `# 今天的回声

写下今天值得被记住的片段。

- 发生了什么？
- 我当时有什么感受？
- 明天想带着什么继续出发？
`;
